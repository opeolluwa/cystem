#include <optional>
#include <vector>
namespace commander {
void init_command(const std::optional<std::string> &name,
                  const std::optional<std::string> &path);
}; // namespace commander
